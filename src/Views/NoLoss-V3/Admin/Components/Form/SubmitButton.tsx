import { BlueBtn } from '@Views/Common/V2-Button';

export const SubmitButton = () => {
  return (
    <BlueBtn onClick={() => {}} className="!w-fit !text-f14 !px-3 !py-1 !h-fit">
      Create Tournament
    </BlueBtn>
  );
};
