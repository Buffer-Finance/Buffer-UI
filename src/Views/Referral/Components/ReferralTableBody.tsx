interface IReferralTableBody {
  row: number;
  col: number;
}

const ReferralTableBody: React.FC<IReferralTableBody> = (row, col) => {
  switch (col) {
  }
  return <div></div>;
};

export default ReferralTableBody;
