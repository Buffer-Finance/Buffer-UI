const TestComponent: React.FC<any> = ({}) => {
  return (
    <div>
      <iframe
        style={{ width: '100vw', height: '100vh' }}
        src="https://testnet.buffer.finance"
      ></iframe>
    </div>
  );
};

export { TestComponent };
